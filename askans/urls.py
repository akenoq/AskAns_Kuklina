#строчки схожи с импортом в urls.py всего проекта
from django.conf.urls import url
from . import views

urlpatterns = [
    #добавляем первый шаблон
    #url(r'^$', views.post_list, name='post_list'),
]